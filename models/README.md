# Models

Model weights and downloaded artifacts belong under this directory in later phases, but they must not be committed to git.

Phase 0 uses only placeholder metadata and does not download models.

