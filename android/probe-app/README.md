# Probe App Placeholder

The Android probe app will be implemented in a later phase after the repository skeleton is stable.

It will collect runtime device capabilities and write probe JSON compatible with `qpnpu.probe_schema`.

